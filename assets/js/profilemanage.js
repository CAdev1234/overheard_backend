
(function($) {

    'use strict';

    /*$('#user_active_btn').on('click', function () {
        let user_id = $('#profile-container').attr('user-id');
        $.ajax({
            url: app_url + '/active_manage_user',
            data: {
                _token: $('meta[name=csrf-token]').attr("content"),
                user_id: user_id
            },
            type: 'post',
            success: function(data) {
                if(data.isActive == 1){

                }
                else{

                }
                new PNotify({
                    title: '',
                    text: 'User Blocked Successfully',
                    type: 'success',
                    shadow: true
                });
            },
            failure: function () {
                new PNotify({
                    title: '',
                    text: 'User Block Failed',
                    type: 'error',
                    shadow: true
                });
            }
        });
    });*/

}).apply(this, [jQuery]);